<style>
    :root {
        --brand: #16a34a;
        --brand-dark: #166534;
        --border: #dbe7d9;
        --bg: #f4fbf4;
        --text: #0b1f0b;
        --white: #fff;
        --radius: 12px;
    }

    body {
        font-family: system-ui, -apple-system, "Segoe UI", Roboto, Ubuntu;
        color: var(--text);
        background: var(--bg);
        margin: 0;
        padding: 0;
    }

    .detail-card {
        background: #fff;
        border: 1px solid var(--border);
        border-radius: var(--radius);
        padding: 2.5rem 2rem;
        margin: 45px auto;
        max-width: 540px;
        box-shadow: 0 2px 8px rgba(0,0,0,.08);
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        box-sizing: border-box;
    }

    .detail-img {
        width: 240px;
        height: 240px;
        object-fit: cover;
        border-radius: 14px;
        border: 1.5px solid var(--border);
        margin-bottom: 1.4rem;
        background: #e6f9e9;
    }

    .detail-title {
        font-size: 2rem;
        color: var(--brand-dark);
        font-weight: 700;
        margin: 0 0 0.6rem 0;
    }

    .detail-info {
        margin-bottom: 1.2rem;
        color: #444;
        line-height: 1.5;
    }

    .pill {
        display: inline-block;
        padding: 2px 9px;
        border-radius: 999px;
        background: #e6f9e9;
        border: 1px solid var(--border);
        font-size: 15px;
        margin: 0 3px;
    }

    .muted {
        color: #666;
        font-size: 1rem;
        margin-top: 1.3em;
    }

    .btn {
        display: inline-block;
        padding: 8px 16px;
        border-radius: 8px;
        border: 1px solid var(--border);
        background: var(--white);
        color: var(--brand-dark);
        cursor: pointer;
        font-weight: 500;
        margin-top: 2em;
        text-decoration: none;
        transition: background-color 0.2s;
    }

        .btn:hover {
            background: #f0fdf4; /* Jemnější hover efekt */
        }

    /* --- Responzivní zobrazení --- */

    /* Styl pro tablety a menší desktopy */
    @@media (min-width: 768px) {
        .detail-card {
            max-width: 640px;
        }
    }

    /* Styl pro mobilní telefony */
    @@media (max-width: 767px) {
        .detail-card {
            margin: 24px 12px;
            padding: 2rem 1.5rem; /* Upraveno pro lepší poměr */
            max-width: calc(100% - 24px);
        }

        .detail-img {
            width: 180px; /* Mírně zvětšeno pro lepší vizuální dopad */
            height: 180px;
        }

        .detail-title {
            font-size: 1.6rem; /* Upraveno pro lepší čitelnost */
        }

        .detail-info {
            font-size: 0.95rem;
        }
    }
</style>